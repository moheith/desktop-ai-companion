const path = require('path');

const APP_ROOT = path.resolve(__dirname, '..', '..');
const VENDOR_CUBISM_CORE_PATH = path.join(APP_ROOT, 'live2dcubismcore.min.js');

const LEGACY_MODEL_PATH_ALIASES = {
  'model/model 1/ai_assistant_model.model3.json': 'model/assistant-legacy/ai_assistant_model.model3.json',
  'model/model 2/mascot-live2d.model3.json': 'model/custom-mascot-v1/mascot-live2d.model3.json',
  'model/model 3/mascot-live2d.model3.json': 'model/custom-mascot-v2/mascot-live2d.model3.json',
};

const DEFAULT_MASCOT_MODEL_PATH = 'model/assistant-legacy/ai_assistant_model.model3.json';
const MASCOT_STAGE_WIDTH = 360;
const MASCOT_STAGE_HEIGHT = 520;

const DEFAULT_CONFIG = {
  mascotX: 1261,
  mascotY: 264,
  mascotWidth: MASCOT_STAGE_WIDTH,
  mascotHeight: MASCOT_STAGE_HEIGHT,
  scale: 0.25,
  mascotVisible: true,
  clickThrough: true,
  borderVisible: false,
  mascotDragShortcutEnabled: true,
  alwaysOnTop: true,
  behindTaskbar: false,
  aiProvider: 'ollama',
  ollamaModel: 'llama3',
  ollamaUrl: 'http://localhost:11434',
  openaiKey: '',
  openaiModel: 'gpt-4o-mini',
  anthropicKey: '',
  anthropicModel: 'claude-3-5-haiku-20241022',
  geminiKey: '',
  geminiModel: 'gemini-2.0-flash',
  nvidiaKey: '',
  nvidiaModel: 'nvidia/llama-3.1-nemotron-70b-instruct',
  customUrl: '',
  customKey: '',
  customModel: '',
  voiceEngine: 'system',
  ttsEnabled: false,
  sttEnabled: false,
  sttEngine: 'openai',
  sttLanguage: 'auto',
  autoMicStop: true,
  autoMicSend: true,
  micDeviceId: '',
  sttSilenceTimeoutMs: 1600,
  sttSensitivity: 0.014,
  sttHoldToTalk: false,
  wakeWordEnabled: false,
  wakeWordPhrase: 'hey mascot',
  sttBilingualBias: 'off',
  sttNoisePreset: 'medium',
  sttMode: 'chat',
  wakeEngine: 'browser',
  porcupineAccessKey: '',
  porcupineKeywordPath: '',
  porcupineSensitivity: 0.55,
  wakeWordAliases: 'hi mascot, hey buddy',
  wakeFollowupEnabled: true,
  wakeFollowupMs: 12000,
  wakeListenAfterReply: true,
  wakeRearmAfterSend: false,
  wakeSensitivity: 0.008,
  wakeSpeechWindowMs: 1400,
  wakeSilenceMs: 260,
  voiceHudEnabled: true,
  voiceInterruptEnabled: true,
  voiceIntentRouting: 'hybrid',
  voiceConfirmActions: true,
  voiceCommandTraining: '',
  ambientNoiseFloor: 0,
  micProfiles: {},
  systemVoice: '',
  elevenKey: '',
  elevenVoiceId: 'EXAVITQu4vr4xnSDxMaL',
  elevenModel: 'eleven_turbo_v2_5',
  piperPath: '',
  piperVoice: '',
  whisperCppPath: '',
  whisperCppModel: '',
  openaiTTSKey: '',
  openaiTTSModel: 'tts-1',
  openaiTTSVoice: 'nova',
  savedProviders: [],
  chatFontSize: 14,
  theme: 'dark',
  mascotModelPath: DEFAULT_MASCOT_MODEL_PATH,
  chatHistory: [],
};

const MIC_RESET_DEFAULTS = {
  sttEnabled: false,
  sttEngine: 'openai',
  sttLanguage: 'auto',
  autoMicStop: true,
  autoMicSend: true,
  micDeviceId: '',
  sttSilenceTimeoutMs: 1600,
  sttSensitivity: 0.014,
  sttHoldToTalk: false,
  wakeWordEnabled: false,
  wakeWordPhrase: 'hey mascot',
  wakeEngine: 'browser',
  sttBilingualBias: 'off',
  sttNoisePreset: 'medium',
  sttMode: 'chat',
  wakeWordAliases: 'hi mascot, hey buddy',
  wakeFollowupEnabled: true,
  wakeFollowupMs: 12000,
  wakeListenAfterReply: true,
  wakeSensitivity: 0.008,
  wakeSpeechWindowMs: 1400,
  wakeSilenceMs: 260,
  voiceHudEnabled: true,
  voiceInterruptEnabled: true,
  voiceIntentRouting: 'hybrid',
  voiceConfirmActions: true,
  voiceCommandTraining: '',
  ambientNoiseFloor: 0,
};

function normalizeMascotModelPath(rawPath) {
  const normalized = String(rawPath || DEFAULT_MASCOT_MODEL_PATH).replace(/\\/g, '/');
  return LEGACY_MODEL_PATH_ALIASES[normalized] || normalized;
}

module.exports = {
  APP_ROOT,
  DEFAULT_CONFIG,
  DEFAULT_MASCOT_MODEL_PATH,
  LEGACY_MODEL_PATH_ALIASES,
  MASCOT_STAGE_HEIGHT,
  MASCOT_STAGE_WIDTH,
  MIC_RESET_DEFAULTS,
  VENDOR_CUBISM_CORE_PATH,
  normalizeMascotModelPath,
};
